# gorkproject

Practice repo for agentic PR demos.

## IT Health status page

Open `status/index.html` locally, or enable GitHub Pages on the `status` folder / `docs` later.

```bash
python3 -m http.server 8080 --directory status
```
Then visit http://localhost:8080
