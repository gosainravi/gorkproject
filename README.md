# gorkproject

Simple hello webpage used to demo agentic PR review.

## View locally

Open `index.html` in your browser, or from this folder:

```bash
python3 -m http.server 8080
```

Then visit http://localhost:8080
